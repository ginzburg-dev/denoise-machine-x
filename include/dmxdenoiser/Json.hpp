#pragma once

#include <iostream>
#include <map>
#include <string>
#include <string_view>

namespace dmxdenoiser
{   
    /*
    class Json
    {
    public:
        Json(std::string_view filename);
        Json(const Json&) = default;
        ~Json();
    
    private:
        std::map<>
    };
    */

} // namespace dmxdenoiser
